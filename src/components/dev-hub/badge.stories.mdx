import { Meta, Story, Preview } from '@storybook/addon-docs/blocks';
import Badge from './badge';

<Meta title="Badge" />

# Badges

Here are the badges. Each badge has a unique color based on its content. The default values are defined below:

```
const badgeTypeColorMap = {
    Article: colorMap.salmon,
    Community: colorMap.magenta,
    'Deep Dive': colorMap.teal,
    Event: colorMap.orange,
    'How-To': colorMap.yellow,
    'Quick Start': colorMap.lightGreen,
};

```
<Preview>
    <Story name="how-to">
        <Badge>How-To</Badge>
    </Story>
    <Story name="quick start">
        <Badge>Quick Start</Badge>
    </Story>
    <Story name="article">
        <Badge>Article</Badge>
    </Story>
    <Story name="event">
        <Badge>Event</Badge>
    </Story>
    <Story name="community">
        <Badge>Community</Badge>
    </Story>
    <Story name="deep dive">
        <Badge>Deep Dive</Badge>
    </Story>
</Preview>
