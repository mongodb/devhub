import React from 'react';
import MediaBlock from '../media-block';
import { P } from '../text';
import GithubBackpackMongo from '../../../images/github-backpack-mongo.png';

const GithubStudentPack = () => (
    <MediaBlock mediaComponent={GithubBackpackMongo}>
        <P>Foo bar</P>
    </MediaBlock>
);

export default GithubStudentPack;
